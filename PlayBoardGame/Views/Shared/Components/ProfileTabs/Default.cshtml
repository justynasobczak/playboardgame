@model ProfileTabsViewModel

<div class="tabs">
    <ul class="nav nav-tabs">
        <li class="nav-item">
            <a class="@("nav-link " + (Model.SelectedTab == ProfileTabName.Details ? "active" : "tab-links"))" 
               asp-controller="User" asp-action="UserProfileAsync"> Details </a>
        </li>
        <li class="nav-item">
            <a class="@("nav-link " + (Model.SelectedTab == ProfileTabName.Password ? "active" : "tab-links"))" 
               asp-controller="User" asp-action="ChangePassword"> Change password </a>
        </li>
    </ul>
</div>